mutation Login($email: String!, $password: String!) {
  login(data: { email: $email, password: $password }) {
    access_token
    refresh_token
    expire_date
    user {
      ...UserFields
    }
  }
}
