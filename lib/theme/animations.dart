enum Animations {
  search("assets/animations/search.json"),
  loading("assets/animations/loading.json"),
  confetti("assets/animations/confetti.json"),
  loadingDots("assets/animations/loading-dots.json");

  final String path;
  const Animations(this.path);
}
