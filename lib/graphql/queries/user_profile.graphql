query GET_USER_PROFILE($username: String!) {
  getUser(username: $username) {
    id
    username
    bio
    avatar
    birthday

    _count {
      followers
      followings
      posts
    }
  }
}
